using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OC_Swipe : MonoBehaviour
{
    /*
    private float animTimer = 0.7f;
    public override void EnterState(EnemyManager enemy)
    {
        animTimer = 0.7f;
        enemy.NavFalse();
        enemy.FacePlayer();
        enemy.SwipeAttack();



    }

    public override void UpdateState(EnemyManager enemy)
    {
        if (animTimer < 0)
        {
            enemy.SwipeBox();
            enemy.justAttacked = true;
            enemy.SwicthState(enemy.IdleState);
            enemy.justSwiped = true;
        }
        else
        {
            animTimer -= Time.deltaTime;
        }
    }

    public override void OnCollisionEnter(EnemyManager enemy, Collision collision)
    {

    }
    */
}
