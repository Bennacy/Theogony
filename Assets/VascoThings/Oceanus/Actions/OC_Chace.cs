using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OC_Chace : MonoBehaviour
{
    /*
    private int nextAttack;

    public override void EnterState(EnemyManager enemy)
    {
        nextAttack = Random.Range(0, 4);
        enemy.ResetNav();

    }

    public override void UpdateState(EnemyManager enemy)
    {
        enemy.WalkAnim();

        if (nextAttack == 0 || nextAttack == 1)
        {
            enemy.DetectionRange(enemy.PunchState, enemy.chaceRange, enemy.chaceStop);
        }
        else if (nextAttack == 2)
        {

            enemy.DetectionRange(enemy.SwipeState, enemy.chaceRange, enemy.chaceStop);
        }
        else
        {
            enemy.DetectionRange(enemy.JumpState, enemy.chaceRange, enemy.jumpRange);
        }


    }

    public override void OnCollisionEnter(EnemyManager enemy, Collision collision)
    {

    }
  */
}
